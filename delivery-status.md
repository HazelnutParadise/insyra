# Delivery Status

> **You are on `0.4` (2026-09-11).** This branch carries the whole-repo API review and its breaking changes: the library no longer terminates on a fatal, `Err()` is sticky, `gplot.SaveChart` returns an error, several methods stopped returning `nil`. `dev` stays on the 0.3.x line so feature releases can ship while the review continues, and `api-review.md`, the review's OpenSpec changes and every fix batch live here only. Non-breaking fixes are backported to `dev` as ordinary commits; merge `dev` into `0.4` after each 0.3.x change so this line keeps them.

## Current Phase
Production-readiness hardening (2026-09-06). Every package's exported surface was reviewed symbol by symbol, then the whole repository was reviewed beyond exported symbols (CLI command behaviour, CCL semantics, internal packages, security, test quality, repo/CI hygiene). The ledger is `api-review.md`; every open finding is a GitHub issue labelled `api-review` with a `severity:*` label (#205–#368). Fixes land in OpenSpec batches: `fix-api-review-batch-1` through `-8`, then changes named for what they fix (listed under Latest Milestones). 58 issues carry the `api-review` label and are open (0 high, 36 medium, 22 low), counted from the tracker on 2026-09-25 rather than from arithmetic on this file; the tracker is the authority. Two of the three defects the review's own work turned up sat outside that label — [#371](https://github.com/HazelnutParadise/insyra/issues/371) and [#373](https://github.com/HazelnutParadise/insyra/issues/373), both now fixed and carrying ledger rows Q-11 and ST-11 — while [#372](https://github.com/HazelnutParadise/insyra/issues/372) (LP-2, which had no issue of its own) does carry the label and is inside the 65.

**What is left needs an owner decision.** Every open `api-review` issue was read on 2026-09-12 and the ones that needed no ruling were worked to zero. Several of the 76 are open only for their decision half, with the decision-free half already fixed and a comment on the issue saying which is which — #269, #273, #280, #294 and #329 were in that shape, and all five are now closed.

`insyra/nn` phase 2 (training), `insyra/ml` v1, and the acceleration package are merged and stable; no feature workstream is active while the review backlog is worked down.

## Stage Objective
Close the `severity:high` issues first, then medium, with one OpenSpec change per batch. Findings that need an API decision (return shapes, `LogFatal`, name-vs-index, `IDataList` with unexported methods, the Google Maps crawler) stay open until the owner decides; they are listed in the issue tracker, not here.

## Active Workstreams
None in flight. Nothing left can be picked up without the owner answering something first.

**Wrong answers waiting on a decision, worth taking first:**

**No `severity:high` issue is open.** #208's K-7 (`IDataList`/`IDataTable` carrying unexported methods, so nothing outside the module can implement them) is the one that used to sit with them and still needs a ruling.

**Awaiting a ruling:** #315 how CLI tokens name a column, and the `AGENTS.md` follow-ups on the 53 CLI commands that still ignore a trailing argument, `Counter()` keeping `int(1)` and `int64(1)` apart.

## Latest Milestones
- 2026-09-26 `err-on-nil-receiver`: `Err()` on a nil `DataList` or `DataTable`, the value a lookup returns when it finds nothing, reported nothing and crashed; the question "what went wrong" was the one that took the program down. It now returns a `nil DataList`/`nil DataTable` error, a fresh value per call. By the owner's ruling only `Err()` is guarded, so every other method on a nil value still fails loudly and the nil cannot travel on unnoticed.
- 2026-09-26 `csvxl-formula-guard`: `ExcelToCsv` and `EachExcelToCsv` guard formula-like text by default like the core writer, since a text cell that is safe in a workbook becomes a formula again in a CSV; `ExcelToCsvOptions{Sheets, AllowFormulas}` replaced the trailing sheet list, by the owner's choice, and the guard itself now lives once in `internal/csv`. CLI `convert` gained `allowformulas` and rejects arguments it does not understand.
- 2026-09-26 `csv-formula-guard-by-default` (#285's open default): CSV output guards text a spreadsheet would run by default, by the owner's ruling. Measured first, the guard wrote the number `-5` as `'-5`, text to a spreadsheet and a string to a program reading it back; it now leaves numbers and number-only text such as `+886912345678` alone, which removes most of what made a default-on guard costly. `AllowFormulas` turns it off; `SanitizeFormulas` from v0.3.3 stays one release as a no-op; the CLI's `save` takes `allowformulas true`.
- 2026-09-26 `embeddable-sealed-interfaces` (#208 and the rest of #228): `IDataList` and `IDataTable` carry a hidden method, so nothing outside the module can implement them, and the review had asked whether to open them or drop them. Measured instead of argued: a caller's type that embeds `*insyra.DataTable` already satisfied them and went into `stats`, but `Merge` insisted on a bare `*DataTable`, and isr's tables failed the interface over two methods it overrides to keep chaining. The owner kept the interfaces sealed and made embedding the supported way to extend. `Merge` now reaches the embedded table through a hidden `coreTable()`; the interfaces list every exported method (27 and 17 were missing) except `ClearErr`, `SetErr`, `Pivot` and `Unpivot`, which isr overrides with its own signatures; `TestInterfacesListEveryMethod` holds that line and names each exception with its reason, as the owner asked. isr's tables and lists now pass straight to `stats`, `plot` and `Merge`. QU-2's duplicated checks in `CAPM` and `Beta` became one helper.
- 2026-09-26 `read-write-names` (the last batch of #213, which closes with it): the CSV and JSON readers and writers had two names each, underscores Go does not use, bools no call site could read (`ToCSV(path, false, true, false)`), and options whose zero value was a file with no header row, which every one of the 38 documented calls turned off. Each now has one name taking an optional options struct (`ReadCSVFile`, `ReadCSVString`, `ReadJSONFile`, `ToJSONBytes`, `ToJSONString`, `ToCSV(path, opts...)`), and the header and row names are `NoHeaderRow` and `HasRowNames` in every options struct, so the zero value is the common file. The old names stay one release as Deprecated wrappers with their old meaning, pinned by a test, and an AGENTS.md follow-up removes them. The risk the owner accepted, and the changelog states in bold: code that passed an options struct without the header field now reads and writes a header row. `ReadExcelSheet` only renamed its bools, because inverting a positional bool would silently flip every call.
- 2026-09-26 `plot-heatmap-point-type` and `csvxl-encoding-like-core` (fourth and fifth batches of #213): a heat map's points were of an unexported type under a constraint misspelled `heapMapAxisValue`, so points built in a loop had no slice type to go in; `HeatMapPoint[X, Y]` and `HeatMapAxis` are exported and the constructors became `NewHeatMapPoint` and `NewHeatMapMissingPoint`, by the owner's choice. `csvxl` read an empty encoding as UTF-8 taken as-is and failed on `"AUTO"`, unlike the core readers; both now mean detection, and the test was run against the old code to show it fails there. Chart widths and heights stay CSS strings so `"100%"` keeps working.
- 2026-09-26 `imputer-refuses-unfillable-columns` and `nn-batchnorm-options` (#213): `SimpleImputer.Fit` with the mean or median now fails for a selected column that is not a number column, naming it and its data type, instead of leaving it unfilled and reporting that only through `Params()[name].PassThrough`, which is now Deprecated; the owner ruled it follows the table fills. The third batch, `nn`, needed one change: training batch normalization took momentum and epsilon by position, and now takes `BatchNormOptions{Momentum, Epsilon}`, zero keeping torch's default. The other `nn` optional parameters each mirror one ONNX attribute and already fit the rule.
- 2026-09-25 `core-settings-batch` (second batch of #213: D-8, E-6, E-7): a table-level fill now records an error for a column the caller named and it cannot fill, naming the column and its data type, while filling every column still skips what it cannot fill; before, `FillWithMean(Name("city"))` on text did nothing and said nothing. `DataTable.FillByInterpolation` takes `extrapolate` before its columns, because the table version could not extrapolate and the CLI dropped the option for tables. `NewSimpleImputer` takes an optional `SimpleImputerOptions{Strategy, FillValue}` with the mean as default. `ShowRange` and its siblings print an error for arguments they cannot read instead of showing everything, and `ShowHead`/`ShowTail` were added beside them. `Sample` keeps `withReplacement` required under the new rule that a setting with no safe default is a parameter. An old CLI test asserted the silent skip the owner ruled against and was changed with the reason written in it. `SimpleImputer`'s pass-through of a selected text column was left alone: it is a designed, reported behaviour and needs its own ruling.
- 2026-09-25 `column-data-type` (asked for while deciding E-7 of #213): nothing let a program ask what kind of values a column holds; `ShowTypes` prints each cell's Go type for a person, and the library answered the question privately several times over by different rules. `DataList.DataType()` and `DataTable.ColDataTypes()` now return `DataTypeNumber`, `DataTypeString`, `DataTypeBool`, `DataTypeTime`, `DataTypeOther`, `DataTypeMixed` or `DataTypeEmpty`, leaving `nil` and `NaN` out; integers, floats and decimals are one number type, and text that looks numeric stays text. The owner chose the names over `ValueKind` and `ValueType` because "data type" is the word Excel, SQL and pandas users already use. The private classifiers were deliberately not rewritten onto it in the same change.
- 2026-09-25 `refuse-extra-optional-values` (first batch of #213): a trailing `...T` or `...XxxOptions` stands for one optional value, but most functions kept the first value and dropped the rest without a word, `finance` kept the last, and the `nn` convolution and pooling constructors threw every option away for the defaults. Under the owner's ruling that more than one value is always an error, twenty core methods, every `finance` function with `opts ...Options`, `nn.NewTape` and three layer constructors, `accel.NewSession`, `plot.SaveHTML` and `datafetch`'s `GetReviews` now refuse a second value through their normal error path; the constructors without an error result keep it and report it from `Param`/`Backward`, `Build` or `Discover`. The tests were written first and failed; the `finance` one was also run against the reverted code. `Rank`, which recorded an error and ranked anyway, no longer ranks. `Show`/`ShowRange` were left for the batch that reshapes signatures, because their `startEnd ...any` is a positional pair, not one optional value.
- 2026-09-25 `selector-follow-through` (no issue; the unfinished part of `one-column-selector`, #225): an audit for #213 found that the selector change had not reached everywhere it promised. The scalers, `SimpleImputer` and the three encoders resolved columns through a helper that threw the lookup's explanation away, so `FitTransform(dt, "Age")` answered `column Age not found` about a column that was there, instead of saying to write `Name("Age")`; five tests written first failed on exactly that and pass now. About thirty documentation and skill examples still passed names as bare strings, five did not compile against the current API, `isr.Row` examples used string keys that the append path reads as Excel indices (a key `"Name"` widens the table to about 247,000 columns), and struct listings, prose and Go doc comments still described name-first lookup. All of it now matches the code; the rewritten `isr` examples were run to confirm they build the tables the text describes. Nothing about how a selector resolves changed.
- 2026-09-25 `exported-functions-are-functions` (K-12 of #211, and the `CAI` part of MK-3): `ToFloat64`, `ToFloat64Safe` and `ReadSlice2D` in the root package and `CAI` in `mkt` were variables holding a function, so any caller could reassign one, and since the library calls `ToFloat64Safe` itself from more than forty places, one assignment changed every t-test, ANOVA and decision tree in the process. All four are now `func` declarations; calls compile unchanged. `ReadSlice2D` and `Slice2DToDataTable` were one function under two names, and the owner kept `ReadSlice2D` so the `Read*` family lists every way to get a table; `Slice2DToDataTable` is a Deprecated wrapper removed next release, recorded as an AGENTS.md follow-up. A test written first parses the module without type-checking and fails on an exported variable whose value is a function literal or a function declared in the module; it listed exactly these four, and a type-checked scan of every package found the same four plus an internal test hook that holds no value, so the cheaper check misses nothing today. It passes now.
- 2026-09-25 `save-writes-excel-sheets` (the decision half of CLI-21, #329, which closes with it): `load` read Excel and `convert` built workbooks, but `save t report.xlsx` failed with `unsupported output file type`, and the library had no way to write a DataTable to Excel at all. A workbook holds many sheets, so the owner ruled that a save names one sheet (`Sheet1` unless told otherwise) and touches nothing else: the sheet is added if absent, and if present the save is refused and the file left byte-for-byte unchanged unless the caller says `if-exists replace`, the rule `save … sql` already followed. `ToExcel(path, ExcelWriteOptions)` and `WriteExcel(w, opts)` are new; a replaced sheet goes back to its original position, the active sheet is restored, and formulas on other sheets that name it read the new values. Sheet names match without regard to case, as in Excel. When no sheet is named and `Sheet1` exists, the save is refused rather than renamed to `Sheet2`, and the message offers both `sheet <name>` and `if-exists replace`. `.xls` is refused with a pointer to `.xlsx`, and `sheet` and `if-exists` are refused for other formats. Six library and five CLI tests were written first and failed before the code existed; four more library tests (case, a formula on another sheet, row names, a non-workbook extension) came after, and the order, active-sheet and refusal safeguards were each removed once to show a test fails without them.
- 2026-09-25 `reader-writer-entry-points` (K-11, C-10, Q-8 of #210; the streaming half of #294's SEC-16): every read and write entry point took a file path, so data that is not on disk — an HTTP response, a zip entry, an `embed.FS` file, an S3 object, bytes in memory — had to go through a temporary file. `insyra` gains `ReadCSV(r, opts)`, `StreamCSV(r, opts, batchSize)`, `ReadJSON` over an `io.Reader`, `ReadExcel(r, …)`, `WriteCSV(w, …)` and `WriteJSON(w, …)`; `parquet` gains `ReadFrom`, `StreamFrom` and `WriteTo`. The path and string versions now call them, so one code path serves both, and nothing existing changed signature. Encoding detection judges a sample, so it works on a reader by reading the first bytes ahead through a buffer. `StreamCSV` holds one batch and stops reading when the loop ends; a test that breaks after the first batch fails if the input is read whole (1,600,009 of 1,600,009 bytes). `parquet.WriteTo` hides `Close` from the Parquet writer, which otherwise closes the caller's writer — shown by a test that fails without it. `csvxl` was left out by ruling, since it converts many files into one workbook, and renaming the underscore functions moved to #213, which reshapes the same functions' parameters, so callers break once rather than twice.
- 2026-09-24 `one-reference-toolchain-setup` (RP-7 of #280, which closes with it): `clustering-parity`, `knn-parity` and `reference-verification` each repeated the same Python 3.12, R release and package installs. They now use one composite action, `.github/actions/setup-reference-toolchains`, and `reference-verification` passes the packages only it needs. A change to install steps is exactly the kind that can make a gate skip and a workflow report green having run nothing, so the first run was checked for real work rather than colour: the installs happened, and the suites took 74 s, 48 s and 285 s where a skip-everything run takes a fraction of a second.
- 2026-09-24 `lint-catches-leaks-and-lost-errors` (RP-8 of #280): golangci-lint ran its default set only. Measured against the whole repository, `gosec` would have raised 450 findings, most of them what a data library does on purpose, so the owner chose the five linters that watch for a resource left open and an error lost: `nilerr`, `bodyclose`, `rowserrcheck`, `sqlclosecheck`, `errorlint`. All 61 findings are resolved. `nilerr`'s one finding was a real defect: `insyra env import` without `--force` guards an overwrite with `isEnvironmentEmpty`, which took an unreadable `config.json` as "empty" and ignored unreadable `state.json` and `history.txt` the same way, so a target it could not read was overwritten. It now fails closed. The first test of that asked `Import` and passed against the old code, because the import also failed later when it tried to write; the tests ask the guard itself. `errorlint`'s 50 `%v` formats became `%w`, so `errors.Is` reaches causes through the library. Its auto-fix needed three hand repairs: missing imports, an identity check in a `parallel` test rewritten with swapped arguments (restored, with the reason), and `ccl.AttachExpr` turned into an unreadable block (rewritten). `bodyclose`'s one finding is a false positive and says so where it is reported.
- 2026-09-24 `sql-stream-is-an-iterator` (no issue; found while doing #273): `ReadSQLStream` documented that a caller who stops early must cancel its context, and the review had marked it a template for `parquet.Stream` (E-9). The remedy did not work. After a break and a cancel, the reader goroutine took its cancellation branch and sent the context's error on an unbuffered channel nobody read any more, so it waited for the rest of the process holding the `*sql.Rows` open, one pooled connection per abandoned stream; a probe that did exactly what the documentation said still had the reader running two seconds later. It now returns `iter.Seq2[*DataTable, error]` and reads chunks in the caller's own goroutine, so there is no goroutine at all and the rows close the moment the loop ends. `ReadSQLChunk` is gone. A test that breaks after the first chunk without cancelling requires the pool's `InUse` to be zero, and fails with the `rows.Close` removed.
- 2026-09-24 `parquet-stream-is-an-iterator` (Q-9 and Q-7 of #273): `parquet.Stream` returned two channels, and a caller who broke out of the loop without cancelling its context left the reading goroutines parked for the rest of the process; the documentation's own example also raced the two channels against each other. `Stream` now returns `iter.Seq2[*insyra.DataTable, error]` and is used as `for dt, err := range parquet.Stream(...)`. It runs the old reader under a context of its own and cancels it when the loop ends, so breaking early leaves nothing running. Three tests were written first against the channel API and did not compile; the one that matters counts goroutines after an early break and was shown to fail with the cancel removed (4 running against 2 before), then pass with it restored. No deprecated channel version was kept, the same call the owner made for `lp`'s `SolveModel`.
- 2026-09-24 `csvxl-respects-file-extension` (C-8 of #269): `csvxl` appended `.csv` to every path that did not end in lowercase `.csv`, on the way in and on the way out. Measured before the change, `CsvToExcel` could not read a CSV named `export.txt`, `DATA.CSV` (the check was case-sensitive) or a file with no extension, and `ExcelToCsv` could not write `report.txt`. Reading now opens the path as written when it names a file and appends `.csv` only when nothing, or a directory, is there, so every path that read before still reads and the three unreadable kinds read too; when neither exists the error names both paths and still satisfies `errors.Is(err, os.ErrNotExist)`. Writing keeps any extension the caller gave and adds `.csv` only to a bare name, which the CLI's `convert` inherits. Seven tests written first, five red: the three unreadable names, both-exist, and the both-paths error, whose first draft passed against the old code because one path is a prefix of the other and had to be tightened to quoted paths before it proved anything.
- 2026-09-22 `one-column-selector` (T-11, MK-3 and T-20's documentation half, of #225): the library had four ways of saying which column, and they disagreed. `GetCol` read an Excel index and then quietly retried the string uppercased as a name; `GroupBy`, `Pivot`, `Unpivot`, `Resample`, the ten window `*Col` methods, the encoders, the scalers, the imputer and the fill family read a name first and an index second; `DataTableSortConfig` carried three fields with a precedence rule and a warning; `mkt`'s three configs carried eight `ColIndex`/`ColName` pairs. So `dt.GetCol("price")` and `dt.CumSumCol("price")` answered differently for the same string on the same table. There is now one selector everywhere: a `string` is an Excel-style index, `Name("price")` is a name, an `int` is a position counting from the end when negative. Six resolvers became one shared lookup, `isr.Name` became an alias of the root's, and every fitted transformer (encoder, scaler, imputer) now remembers its column the way it can be found again, by name when it has one and by position when it does not. The core accessors keep their explicit spellings and gained the missing ones; the analysis family deliberately gained none, because the selector already says which kind it was given. Column names take no part in resolution and shape only the failure, which offers the `Name(...)` form when the table has that name; a column named like the index given keeps the index and warns naming both readings. The whole suite passes, `golangci-lint` is clean, and the CLI keeps resolving its own tokens name-first until #315 decides its convention.
- 2026-09-21 `ccl-identifier-is-only-an-index` (CCL-1 of #341, the last `severity:high`): a bare word in CCL is an Excel-style column index and nothing else, on both sides of an assignment. The implementation carried two undocumented fallbacks to the column-name map, one in `Bind` and one in the assignment target, and both fired only when the word failed to parse as letters — so `qty_1 * 2` resolved by name while `price * 2` never could, and the rule a reader saw depended on whether their column name had an underscore. Both fallbacks are gone. The name map is still read, but only to write the failure: on a table with a column named `price`, `price * 2` now says that `price` reads as column index PRICE past the last of 2 columns and that the column is written `['price']`, so resolution never depends on the data while the message still teaches the fix. Four tests written first cover the two failing shapes, the message when no column has that name, and the assignment target. The whole suite passes unchanged, which is itself the evidence that nothing used the fallbacks on purpose.
- 2026-09-20 `mnist-proof-compares-runs` (no issue): the first run of the new data-gates job failed on a real defect. `TestSequentialMNISTConvergence` proved that Sequential changes nothing by comparing its loss curve against `0.350281`, `0.163855` and an accuracy of exactly `95.84%`, all transcribed from an M3. On `ubuntu-latest` the second epoch's mean loss is `0.163840` and the accuracy `95.90%` — and the hand-written tape run in the same job produced `0.163840` as well, so the two paths still agree digit for digit and only the transcription was platform-bound. The test now runs the hand-written loop in the same process under the same seed and requires both epochs' losses and accuracies to be identical between the two; what stays absolute is what any platform meets (the second epoch below half the first, at least 95%). Shown to fail: seeding the hand run one apart reports `epoch 1 mean loss: Sequential 0.350281, hand-written tape 0.356296`. The proof costs a second training run, 63s in CI against 32s before. `TestSequentialFitMNISTConvergence` still pins transcribed numbers and is recorded in the AGENTS.md follow-ups.
- 2026-09-20 `nn-data-gates-in-ci` (TS-5 of #303): `nn`'s MNIST convergence tests and the five-checkpoint parity comparison had never run in CI, because both are gated on a data directory no workflow set and the data — 288MB of published checkpoints, 11MB of MNIST — cannot live in the checkout. The `Neural Network Data Gates` workflow fetches every file from a source pinned by sha256 in `.github/nn-data-manifest.txt` (a commit in `onnx/models`, a Hugging Face revision, PyTorch's MNIST mirror), caches them by the manifest's hash, verifies all nine before testing, and fails when any of the five tests skipped rather than ran. It runs on changes under `nn/`, on its own files and on manual dispatch, and on no schedule: the owner ruled on 2026-09-20 that a weekly run would re-prove code nobody touched. Measured: 117.7s for the five tests on an M3 and 451s on `ubuntu-latest`, where a CNN epoch costs 107s against 31s locally; the whole job takes under nine minutes with nothing cached, and the data cache is 265MB compressed (run 35520530626). The GPU gates still have no runner and stay manual, now written into `ENG.md`.
- 2026-09-17 `factor-parity-compares-invariants`: the strict parity suite held Phi, structure and scores to 2e-5 whenever the aligned loadings agreed within 2e-5, and loosened them to 5e-3 only when the loadings differed by more, so the closer the loadings the stricter Phi was held, although on a flat rotation criterion Phi moves more than the loadings. 308 of 691 failing leaves were these fields, and in all 125 of their combinations L·Phi·L' and S·L', which a rotation leaves unchanged, agreed with psych to 6.2e-6. GPArotation 2026.8.2 from 40 random starts at one minimum spreads 3.1e-3 in Phi on `two_blocks` (ML, oblimin) and 6.0e-3 on `three_blocks` (MINRES, bentlerQ), the gap measured against psych there. Following the research report (GPArotation and EFAtools loosen loadings and Phi together for the same minimum, and nobody loosens Phi alone), the loadings now decide whether the two are the same solution, every frame field of the same solution is compared at `factorRotationTol`, raised from 5e-3 to 1e-2, and the criterion only has to be no worse. L·Phi·L' and S·L' against psych at 2e-5, W·L' for regression and Bartlett scores at 1e-4 (4.5e-5 measured, the extraction's 6e-6 through R⁻¹) and our S = L·Phi at 1e-10 run for every rotation and verdict, a different lower minimum and Promax included. A unit test without R shows the invariants ignore rotation, reordering and sign changes and catch a sign error in Phi or a structure never multiplied by Phi. The suite goes from 691 failing leaves to 512 of 52,176: 313 frame-field leaves pass, and 134 invariant leaves newly fail, every one in the 52 `near_collinear` combinations whose extraction already drifts.
- 2026-09-17 `factor-baselines-reproducible`: the strict parity suite's R references were not reproducible. `psych::fa` rotates from 20 starts, 19 drawn from R's random number generator, and `crosslang_baseline.R` never seeded it, so three runs of the script on one payload (`two_blocks`, ML, oblimin, regression scores) returned Phi up to 5.3e-5 apart while the loadings agreed to 3.5e-6, beyond the suite's 2e-5. `factor_analysis_stats` now seeds the generator before calling psych; a test runs the script twice without the cache and requires identical output, and it failed first. Regenerating the whole cache twice from scratch gave 2,411 byte-identical baselines and the same failing leaves. With seeded references the suite reports 691 failing leaves instead of 709, 64 out and 46 in, all factor-frame fields. Reading psych 2.6.5's `faRotations` for this found a defect in psych itself: a hyperplane tie is broken with a position inside the tied subset rather than a start index (with starts 1 and 3 tied and start 3 less complex, it picks start 2), and the third tie-break is computed without being assigned. The port selects by criterion value and does not share it; the script's comment records it.
- 2026-09-17 `simplimax-follows-gparotation` (ST-15, no issue): simplimax minimised a different criterion from psych's whenever three or more factors were fitted or squared loadings were equal. psych calls `GPArotation::simplimax` without a `k`, which adds up exactly the `k` smallest squared loadings with `k` = variables × (factors − 1), taking ties by column-major position; the port fixed `k` at the number of variables and also counted every squared loading tied with the `k`-th. On the 6×3 pattern the gradient tests use, the criterion at the start was 0.125 where GPArotation's is 0.245, and the rotation drove it to 3e-9 where GPArotation stops at 0.0218. `vgQSimplimax` now selects exactly `k` by a stable column-major sort, and the rotation, the choice among starts and `fa.Criterion` share one default `k`. Tests pin GPArotation's criterion and gradient on four matrices at two values of `k`, and the simplimax rotation cases now come from `GPArotation::simplimax(A, Tmat)`, the call psych makes; both failed first. The strict parity suite stays at 709 with the same failing leaves. Its simplimax combinations on `three_blocks` and `cross_loading` passed before and still pass, now as a different, lower minimum: under GPArotation's criterion psych's answer sits at 0.101 to 0.668 and our search reaches 0.0068 to 0.236, because psych ranks its starts by hyperplane count.
- 2026-09-14 `rotations-use-gparotation-bb` (ST-14, no issue): every gradient projection rotation stepped the way GPArotation's old function does, while GPArotation 2026.8.2 runs every criterion with its `"bb"` algorithm and a cap of 2000, and psych 2.6.5 calls it with those defaults. On the parity suite's two ten-row tables fitted with two factors, 400 random starts converged within 1000 iterations for 7 and 31 (quartimin) and 0 and 3 (geomin) under our port, 6 and 13, and 0 and 2, under GPArotation's retained `GPFoblq.legacy`, and 400 under `"bb"`. From 120 identical starts our port matched `GPFoblq.legacy` at every iteration, so it was faithful to an algorithm the reference had left. `GPForth` and `GPFoblq` now take the Barzilai-Borwein step size held in [1e-10, 20], accept a trial against the largest criterion value of the last ten iterations, and invert a singular oblique matrix through its singular value decomposition; the cap is 2000 in `FaRotations`, `fa.Rotate` and Promax's Varimax. Tests pin 49 cases from GPArotation 2026.8.2: the first six iterations agree to 1e-10 on arm64 and amd64, and later ones drift by rounding, which the step size carries forward, so a long run can end a few iterations from R's. The strict parity suite went from 922 failing leaves to 709: `rotation_converged` 99 to 0, Promax 246 to 76, and no simplimax "worse minimum than R's". 196 leaves newly fail, all structure, Phi and score fields of rotations whose loadings agree with psych's within 2e-5; in R itself `|Phi12|` spans 3.3e-3 over 40 converged starts of the same minimum on `two_blocks` with ML and oblimin, so psych's unseeded starts decide the reference there. Found on the way and left for the owner: simplimax's criterion uses `k` = the number of variables and counts ties, where GPArotation uses variables × (factors − 1) and exactly `k` (`AGENTS.md` follow-up).
- 2026-09-14 `run-cvxpy-reference-in-ci` ([#302](https://github.com/HazelnutParadise/insyra/issues/302), TS-4): the portfolio comparison against cvxpy runs in CI for the first time. It was opt-in because cvxpy is usually absent, and strict mode is meant to turn it on, but the one strict workflow never ran `./quant/` or installed cvxpy. Reference Verification now installs cvxpy and runs `TestPortfolioAgreesWithCVXPY`; run 34838428857 passed it in 2.3 seconds on ubuntu. Locally it passes against cvxpy 1.7.5 with a largest objective difference of 4.09e-09 over 40 problems, and with no Python that has cvxpy it fails naming what is missing instead of skipping. A check of every strict-gated test against the workflow's `-run` patterns found no other gated comparison left without a step.
- 2026-09-13 `parallel-runs-and-worker-errors` ([#271](https://github.com/HazelnutParadise/insyra/issues/271), P-1, P-2, P-4, P-5): `parallel` stays, as the owner ruled, and its defects are fixed inside the `GroupUp(...).Run().AwaitResult()` shape. Measured on the old code the same day: running a group twice re-ran every function into the same slots, two concurrent `Run` calls were a data race under `-race`, awaiting a group nobody ran returned empty results silently, and a panic left an `error` in its slot that looked like one the function returned. `Run` now returns a `*RunningGroup` with only the two await methods, so each run keeps its own results and awaiting before running does not compile. `AwaitResult` returns `([][]any, error)`, and a panic or an uncallable value leaves its slot nil and comes back as a `*WorkerError` with index, panic value and stack. `mkt.RFM` and `stats.RepeatedMeasuresANOVA` check that error. `Docs/parallel.md` is rewritten around three examples the test suite now runs; its old no-return example raced on a shared counter. Tests for every case were written first and failed to compile on the old API; `go test -race` passes on `parallel`, `mkt` and the ANOVA tests.
- 2026-09-13 `rotation-starts-same-on-every-platform` (ST-13, no issue): the `Test` workflow on `0.4` had failed on ubuntu and windows since 2026-09-12 in `TestObliminRestartsSearchTheCriterion`, while macOS passed. The extra starts were used on amd64, but they were different starts. `buildStarts` seeded its random starts from a hash of every bit of the unrotated loadings, and ML extraction of the test's table differs in the eighth decimal between amd64 and arm64 (0.39670959426 against 0.39670957382), so the two drew unrelated streams and first reached the lower basin at `Restarts` 7 and 5. The random starts now come from a fixed seed, and on amd64, amd64 `noasm`, arm64 and arm64 `noasm` all eight starts land in the same basins. The test was right and is unchanged. A new test in `fa` fails on the old code on both architectures. The strict R parity suite moves from 873 to 922 failing leaves, and seeds 2 to 5 give 879 to 906. Nearly all of it is `rotation_converged` on the two ten-row tables, and hash seeds proved no luckier than integer seeds over 60 of each. Run under amd64, the suite agrees with arm64 on every convergence and minimum verdict. The cause first given for that row, nearly one-dimensional data, was wrong: the rotations ported GPArotation's old step, and `rotations-use-gparotation-bb` found and fixed it the next day.
- 2026-09-13 `csvxl-batch-failures` ([#267](https://github.com/HazelnutParadise/insyra/issues/267), C-1): `CsvToExcel`, `AppendCsvToExcel` and `EachCsvToOneExcel` keep converting past a CSV that fails, as the owner chose, and now name every failure. Measured on the old code the same day: a missing CSV left an empty sheet and the error "1 files failed to convert", and `AppendCsvToExcel` emptied the existing sheet of that name before failing and saved the workbook. Each CSV is now read, decoded and checked against the sheet size limit before its sheet is created or replaced. The error lists one line per failed file with its cause, so `errors.Is(err, os.ErrNotExist)` works, which the `io-error-hygiene` scenario for a missing CSV had required without being true. A sheet name Excel rejects fails only its file, and nothing is saved when nothing converted. Seven tests, written first and failing on the old code, cover each case.
- 2026-09-13 `lp-pure-go-default` ([#257](https://github.com/HazelnutParadise/insyra/issues/257), [#372](https://github.com/HazelnutParadise/insyra/issues/372), LP-1, LP-2, SEC-9, SEC-19): `lp` solves with go-milp v0.2.0 by default and never installs GLPK. `Solve` and `SolveFile` return `(*Solution, error)`, with a status for infeasible, unbounded and time-limited models, and replace `SolveModel` and `SolveFromFile` outright on `0.4`. GLPK stays as `EngineGLPK`, which runs an installed `glpsol` found through `GLPK_PATH` or `PATH`, and the Ubuntu and macOS CI legs now install it so the GLPK tests run there. Both engines read models through one CPLEX LP reader ported from GLPK 5.0's `cplex.c`: 20 malformed files get glpsol's own message and line number, and 11 edge-case models plus 9 recorded fixtures reach the same status and objective on both engines. The reader departs from GLPK in one place, `x >= -foo`, which an inverted test in GLPK reads as "no lower bound". Left open upstream: go-milp skips a node whose relaxation fails numerically and still reports the result optimal ([go-milp#2](https://github.com/daniel-sullivan/go-milp/issues/2), filed with a reproducer), which `Docs/lp.md` states.
- 2026-09-13 `require-go-1-26`: the `0.4` line's `go` directive moves from 1.25.12 to 1.26.8, the newest 1.26 release on go.dev that day. The owner decided it so `lp` can depend on go-milp, whose `go.mod` declares `go 1.26` (#257, #372); `dev` stays on Go 1.25. `go mod tidy` changed nothing else, govulncheck scans with `1.26.x`, and the docs index and tutorials say Go 1.26+. The chromedp follow-up's Go precondition is now met on `0.4`; its retry is still separate.
- 2026-09-13 `gmaps-review-extra-fields`: the owner asked for the facts the new review pages carry and `GoogleMapsStoreReview` had no field for. `ReviewID` (`[5]`), `Language` (`[26]`), `ReviewerReviewCount` (`[3][3]`) and `ReviewerPhotoCount` (`[3][4]`) are filled by `GetReviews` and become `ToDataTable` columns. The counts were checked against the numbers the Search review window shows for the same reviewers, and the positions against all 20 saved reviews.
- 2026-09-13 `gmaps-reviews-restored` ([#249](https://github.com/HazelnutParadise/insyra/issues/249), DF-1): `GetReviews` works again. Signed out, every Google Maps route stops at five reviews: `listugcposts` answers 403, the place page's review list loads no more when scrolled and says it shows only part of the content, and `batchexecute` rpc `qv9Egd` returns five per sort order with a session token that works about one time in three. `/async/reviewDialog`, the route scraping guides describe, is 404. The review window on Google Search results is not limited: scrolling it loads 10 more at a time through `GET /httpservice/web/PrivateLocalSearchUiDataService/GetLocalBoqProxy`, whose `reqpld` carries the store, the sort order (1 to 4, the existing `SortBy` values), a page size of 10 and a page token. Replayed with no cookies it walked six pages, 60 distinct reviews, and answered for a second store. `GetReviews` keeps its signature and meaning; `ReviewerState` and `ReviewerLevel` are empty because the pages no longer carry them. Two leftovers from the exploration: a review agent was stopped by the auto-mode classifier and left two background loops, which were killed, and one of its scripts called ipify to read its public IP.
- 2026-09-13 `gmaps-search-restored` ([#249](https://github.com/HazelnutParadise/insyra/issues/249), DF-1, SEC-14): the owner chose to repair the Google Maps crawler rather than remove it, fixing what is known to be fixable first. `Search` returned 0 stores for every query because Google no longer puts store IDs in the page it read; it now replays the `/search?tbm=map` request the Maps page makes, with the page's 1,643-character `pb` descriptor cut to 99 characters that return the same stores (checked on 鼎泰豐, 11, and 星巴克, 20). The runtime download of URLs and headers from a personal GitHub repository is gone, requests time out after 30 s, and `GetReviews` no longer prints to stdout or panics at a 1000 ms waiting interval. Reviews are still broken: `listugcposts` answers 403, the Maps page now loads reviews through `batchexecute` rpc `qv9Egd` whose request body has not been captured, and logged out, filtering reviews raises a sign-in wall and scrolling loads no more, so paging and sort may not be reachable without an account. #249 stays open for that.
- 2026-09-13 `sortby-empty-config-first-column` ([#233](https://github.com/HazelnutParadise/insyra/issues/233)): the owner reversed the refusal in `sortby-column-selection`. `SortBy` only runs when someone asked for a sort, so sorting a config that names no column by the first column is not a sort nobody asked for, and refusing it also refused `{ColumnNumber: 0}`, the same Go value. `{}`, `{Descending: true}` and `{ColumnNumber: 0}` sort by column 0 with no error and no warning, and `Docs/DataTable.md` says so. Precedence keeps `ColumnNumber` last and counts it only when non-zero, so a name or an index is never overridden by the zero value. The rest of `sortby-column-selection` stands, and nothing about `SortBy` in the unreleased changelog is breaking any more.
- 2026-09-13 `sortby-column-selection` ([#233](https://github.com/HazelnutParadise/insyra/issues/233), T-23): `DataTableSortConfig{}` and `{ColumnNumber: 0}` are the same value in Go, so a config that named no column sorted by the first one. The owner chose a check over a struct change: a config that names no column is refused, and the first column by position is `ColumnIndex: "A"`, which the existing doc sentence "at least one … must be specified" already implied. Measuring every shape of config found more: an out-of-range `ColumnIndex` did nothing with a nil `Err()`, a missing name or number reported an internal lookup instead of `SortBy`, and a multi-level sort with one bad level applied the others. All three are errors on `SortBy` now, and every level is resolved before any row moves. Several selectors in one config are not an error: the owner ruled they sort by the documented precedence — index, then name, then number, the rule `mkt` follows — with a warning.
- 2026-09-13 `promax-matches-psych`: Promax was the strict parity suite's largest remainder (1,028 of 1,672 leaves). Stepping psych 2.6.5 and ours side by side on the suite's ten-row table found the loadings and the Kaiser weighting identical and the first divergence inside `Promax`: psych 2.6.5 replaced `stats::varimax` with `GPArotation::Varimax` as `psych::Promax`'s pre-rotation ("replaced with GPArotation Varimax 5/9/26"), while `principal()` still uses `stats::promax` and its `stats::varimax`. On a nearly flat varimax criterion the two stop at different angles and Promax raises that to the fourth power: loading[0,0] 0.700 against 0.625. `fa.Promax` now carries both pre-rotations, `normalize = false` for psych::Promax (the `fa()` path) and `true` for stats::promax (the PCA path). Measured against psych 2.6.5 over 16 datasets and four extractions: PCA to 4e-13, MINRES/ML/PAF to 6.4e-4 at worst, where it was 0.29. The suite reports 875 leaves; Promax's 233 are the two ten-row tables at 1e-4 to 6e-4 and near-collinear ML extraction drift.
- 2026-09-12 `a-decimal-is-a-number` (FI-2 of [#247](https://github.com/HazelnutParadise/insyra/issues/247)): `Mean`, `Sum`, `Describe` and every `stats` entry point answered `NaN` with no error on a column of money, because the numeric path could not read a fixed-point decimal — and two places in the library put one in a cell, `finance.ScheduleTable` and a Parquet `Decimal128` column. The owner ruled that a decimal is a number. `IsNumeric` moved with the read path, because the split between them is what `fix-clear-defects-core` closed for named numeric types. Sorting moved too: a decimal sat at rank 5, after strings, so a mixed column came out `1, 3, 2.50, 10.00` instead of interleaved; two decimals still compare through `decimal.Cmp`, so a difference beyond `float64`'s 16 digits is still seen. **Matched by shape, not by type**: a value reporting its own text and its scale, whose text parses as a number, so `internal/utils` — which every value in the library passes through — depends on no decimal package, and FI-1's complaint is not spread from `finance` into core.
- 2026-09-12 `print-a-key-as-the-value-writes-itself`: a stand-in key rendered the encoded content, which for a struct is its fields, so a decimal printed as `decimal.Decimal({{b:1,[i:3400221114815]},i:10})` — `big.Int`'s sign and words — for a value that knows it is `-340.0221114815`. It now prefers the value's own `String()`, which reaches both places the library already puts a decimal in a cell: `finance.ScheduleTable` and a Parquet `Decimal128` column. Identity still comes from the encoding, because a `String` may be lossy and two distinct values whose text matched must not merge; the text field takes part in `==` and the reasoning for why that is safe is written next to it.
- 2026-09-13 `factor-parity-compares-what-it-means`: the strict R parity suite for factor analysis compared rotation matrices, loadings and `Phi` element by element, judged a multimodal rotation by which minimum psych's unseeded starts happened to reach, and keyed its baseline cache on script and payload alone. Against psych 2.6.5 and GPArotation 2026.8.2 that was 5,334 failing leaves, 1,408 of them a rotation matrix off by a column swap or a sign. It now aligns the factors once from the loadings and applies that to every factor-indexed field, compares a rotation matrix by the loadings it produces (an oblique one is `(T')⁻¹` and reaches entries of 12 at `Phi = 0.9965`), judges a gradient-projection rotation by its own criterion (`fa.Criterion`, new) — same minimum within a relative 1e-4 compared at `factorRotationTol = 5e-3`, a lower one passes, a higher one fails — and hashes the R and Python package versions into the cache key. **1,672 leaves fail now, and each part is named at `factorParityTol`**: Promax 1,028 (its own follow-up: a different solution from psych's on 11 of 16 datasets), extraction drift on the adversarial datasets ~260, anderson-rubin scoring combinations that fail before any field 99, rotations out of iterations on near-collinear data 50, and six simplimax combinations where twenty starts found a worse minimum than psych's. Measured along the way: tightening our rotation eps to 1e-7 moves 23 of the 351 same-minimum combinations and leaves 132 rotations unconverged, so the 1e-4 gap at a shared minimum is the criterion's curvature, not our precision.
- 2026-09-12 `nan-is-not-a-map-key`: `identify-uncomparable-cells` set the rule that counting and searching give the same answer, and NaN broke it — which matters because a NaN in a numeric column is what a missing value reads as, not a corner case. `float64` is comparable to Go, so a NaN went into `Counter`'s map as its own key, and since `NaN != NaN` every one made an entry nobody could read back: three NaNs reported three counts of one while `Count` correctly said 3. The guard was asking the wrong question. `reflect.Value.Comparable` asks whether `==` is legal; a map key needs it to be *useful*, which is **whether the value equals itself**. That one test also catches the nested shapes, measured: `[2]float64{NaN, 1}` and a struct with a NaN field both report comparable and both are unequal to themselves, while the same struct without the NaN is a perfectly good key.
- 2026-09-12 `parquet-binary-is-bytes`: a Parquet `Binary` column now reads as `[]byte`. It read as a `string` because a cell could not be a slice and a `[]byte` cell did not work, and both of those were fixed by `identify-uncomparable-cells` and `one-value-one-cell`. What the string form left behind was the defect: measured side by side, a `Binary` column and a `String` column holding the same bytes came back with the same Go type and equal values, so nothing could tell them apart, a round trip turned the binary column into a string one, and the rendering followed the data rather than the column — `'A-01'` on one row and `00ff41` on the next, because the invalid-UTF-8 hex rule fired per cell. `Show` now renders the whole column as hex, `Write` emits a `[]byte` column as Arrow `Binary`, and JSON export is base64. The column builder appends a `[]any` instead of passing it to `NewDataList`, because the constructor would otherwise flatten each `[]byte` back into its bytes.
- 2026-09-12 `one-value-one-cell`: `NewDataList` flattens slices on purpose, so a list reads the way a pandas Series does, and there was no way to opt one argument out short of leaving the constructor for `Append` — which never flattens but cannot be mixed into the same call. `Cell(v)` is that opt-out, inline among ordinary values, and the mark is removed on the way in so the cell holds the value at its own type. Every entry point that takes a value from the caller unwraps it, the ones that never flatten included, so `Append(Cell(x))` and `Append(x)` agree and writing the first for consistency is not a trap; the search side unwraps too, so `Count(Cell(x))` matches. A bare slice still flattens, which the owner ruled on rather than leaving implicit.
- 2026-09-12 `identify-uncomparable-cells`: a cell value Go cannot compare used to crash the library and go missing at the same time. `Counter` keyed a `map[any]int` by the cell, so an unhashable value panicked there, and `equalCell` answered "not equal" for anything uncomparable, so `Count`, `FindAll`, `Replace` and `IsEqualTo` never found it. **Reachable without trying**: `convertSQLValue` keeps a binary column as `[]byte` on purpose, so reading a BLOB column and calling `Counter` took the process down while `Count` returned 0 for a value sitting in two rows, reproduced end to end against sqlite. One rule now covers it — a cell Go cannot compare is identified by its type and content — through a new `encodeCell`, the exported `UncomparableKey` and `ToMapKey`. Three things had to be true and none was optional: the encoding had to recurse (the old `%T:%v` fallback made `[]any{1}` and `[]any{"1"}` one group in `GroupBy`, `Pivot` and `Merge`, and a stand-in key built on it would have inherited that on day one); the comparability test had to be `reflect.ValueOf(v).Comparable()`, because `TypeOf(...).Comparable()` reports true for `[2]any{[]int{1},2}` and `struct{X any}{[]int{1}}` and both still panic on `==`; and `Count` had to move with `Counter`, or the same value would be counted twice by one and not found by the other. Scalar group keys are unchanged byte for byte. Fable was consulted on the naming and on whether the recursion belonged in this change; both of its rulings were taken, and its correction of the `TypeOf`/`ValueOf` mistake is the reason the fix works on the two shapes I had missed.
- 2026-09-12 `one-warning-per-unconverged-rotation`: `GPForth`/`GPFoblq` warned on every start that hit its iteration cap, and `insyra.LogWarning` pushes into the global error buffer, so with twenty starts a geomin or simplimax rotation could log twenty warnings and leave twenty buffer entries while the chosen solution had converged — the informed Varimax start warned too. Per-start runs now report at debug level; `FaRotations` warns once, naming the method, start count and cap, only when the solution it chose did not converge. Pinned by two tests that capture the logger and count buffer entries.
- 2026-09-12 `default-restarts-follow-psych`: the two questions `oblimin-honours-its-start` left, decided from the three R sources (GPArotation 2026.8-2 `R/GPFRS.R`, psych 2.6.5 `R/faRotate.r` and `man/fa.Rd`, fungible `R/faMain.R`). The rule stays lowest-criterion-among-converged: GPArotation's engine and fungible select the minimum, psych's hyperplane count chose the same basin on every fixture measured, over-factored included. The default becomes 20: psych's manual calls multiple restarts "strongly encouraged (see Nguyen and Waller)" and its author changed the default on a real local-minimum example; fungible defaults to 10; `ENG.md` names R as the reference and the baseline script calls `psych::fa` with its defaults. Measured on the twenty datasets in the suite, default loadings move by at most 1.2e-5 and 240 of 240 oblimin starts converged. **The cost was not the search**: twenty oblimin starts take 1.5 to 9 ms, while the informed Varimax start at `eps 1e-8 / maxit 5000` hit the cap on five datasets at 76 to 86 ms each. It now runs at the rotation's own tolerance, 0.03 ms.
- 2026-09-12 `oblimin-honours-its-start` (ST-12, no issue): `FaRotations`'s oblimin arm built its own identity start on every pass and ignored the one it was handed, so `Restarts: 20` ran the same computation twenty times — measured, 202 ms to return the 9.9 ms answer — while `Docs/stats.md` promised a search. Decided on the statistics and on the R references rather than on the comment's SPSS claim: `Restarts` is a multi-start minimisation and a start that is not used is not a start; `psych::faRotations` (2.6.5) passes `Tmat = initial` to `GPArotation::oblimin` with no per-method exception, and `GPArotation::oblimin()` has `randomStarts`. SPSS is single-start (IBM Algorithms, FACTOR: "C is initialized to Im"), which is `Restarts: 1`, left bit-identical — measured on eight configurations and structural, since `buildStarts` returns the identity alone. Measured on the review's synthetic table: with three factors every start reaches the same minimum (`f` moves by 4e-12); with four, a random start reaches a basin 47 times lower (0.0444 to 0.00094) whose `Phi` has an off-diagonal of 0.905, so the docs now say to look at `Phi` before preferring a multi-start result. A threshold before a random start may displace the identity was considered and rejected: neither reference has one. Reading both references also found that our selection rule follows GPArotation's engine (minimum `f`) while psych ranks by hyperplane count, and that psych 2.6.5 changed `n.rotations`'s default from 1 to 20; both are a follow-up.
- 2026-09-12 `parquet-foreign-column-types` ([#371](https://github.com/HazelnutParadise/insyra/issues/371), Q-11): `getVal`'s fallback returned `arr.String()`, the string form of the whole array, ignoring the row index, so a column type the reader did not handle read back as one identical string in every row with `Read` returning a nil error and `Err()` nil. Invisible on files this library wrote, because `Write` emits only seven Arrow types. Measured by writing nine foreign types and reading them back: eight were wrong, and **Dictionary was not among them** despite the issue listing it, because pqarrow materialises it as its underlying type — which mattered, since a pandas `category` column was the most common case on that list. Twenty Arrow types now have a faithful Go representation, including `Decimal128`/`Decimal256` through [go-decimal](https://github.com/TimLai666/go-decimal), whose `big.Int` coefficient and `int32` scale carry the full 38-digit range exactly (an `int64` coefficient could not). The rest read as `nil` with the column name and Arrow type on the table's `Err()`. Two things the implementation ran into: a DataList cell cannot be a slice, because `NewDataList` flattens every `reflect.Slice`, so `Binary` reads as a `string` holding the raw bytes rather than a `[]byte`; and ordering a decimal correctly needs `decimal.Cmp`, with no interface to dispatch on, so `internal/algorithms` imports go-decimal and it is a core dependency rather than a `parquet`-only one. Without that branch a column of 9.5, 10.2 and 100.0 sorted 10.2, 100.0, 9.5.
- 2026-09-12 `orthogonal-rotation-starts` ([#373](https://github.com/HazelnutParadise/insyra/issues/373), ST-11): `FactorAnalysis` with `Rotation.Restarts > 1` returned loadings that no longer described the fitted model. A rotation criterion is minimised over a constrained set, and the gradient projection algorithm only keeps an iterate on it if the start is on it; two of the starting points added for `Restarts > 1` were Promax and Target matrices, which are oblique. **The defect was wider than the issue**: it is not confined to the orthogonal family, because `FaRotations` premultiplies the loadings by the start instead of passing it to the algorithm, and the oblique parameterisation is `Λ = A·(T')⁻¹`. Measured on the review's own synthetic table, four of ten methods were wrong at `Restarts ≥ 2` (`max|L·L' − Lu·Lu'|` = 0.287, matching the issue), and BentlerQ broke its own oblique invariant by 0.763. Every start is now orthogonal and verified before use, and `Restarts` is the number of starts rather than a bound on the random ones only. A second defect in the same loop came with it: the candidate chosen among restarts never carried the convergence flag, so `RotationConverged` was a constant true — all nine iterative methods reported convergence after one iteration at a tolerance of 1e-12. Three tests were repaired along the way: the one that pinned the defect and asked to be replaced, `TestRestartsParameter` which ran GeominQ alone and so only ever checked the oblique invariant, and `TestRotationConvergedFlag` which tried to cripple a rotation with `opt.MaxIter` (extraction only, never reaches the rotation) and wrote its assertion as a `t.Logf`.
- 2026-09-12 `docs-hygiene-and-remaining-partials` (#232, #274, #281, #282, RP-6 of #280, the decision-free halves of #244, #269, #273, #329): the last of the backlog that needed no ruling. Nineteen `DataTable` methods had no doc comment and two named the wrong function, one of them in the text `Err()` hands back; `parquet`'s comments were in a colon style Go does not use and three of its types had none; CI mixed two major versions of `checkout` and `setup-go` and pinned golangci-lint to `latest`, so a lint result could not be reproduced. Two behaviour fixes came with it: `ExcelToCsv` reports a named sheet the workbook does not have instead of quietly producing fewer files, and `read x.csv as v` says to use `load` rather than `unknown option "as"`. The skill gained `references/stats.md` and `references/plotting.md`, the two packages it is asked about most. What was deliberately left: RP-7 (one composite action across three workflows) and RP-8 (which linters to add), and the decision halves of the four partial issues.
- 2026-09-12 `cli-message-and-help-fixes` (#324, #326, #327, #296): `ttest single x abc` answered with `strconv.ParseFloat: parsing "abc": invalid syntax` and twenty other places did the same; `kmeans` refused `NSTART` and listed nothing; five commands reported a missing variable that was right there holding the wrong type; the help table was a fixed twelve characters wide and read the registry without its lock; and nine regular expressions were compiled on every call.
- 2026-09-12 `harden-limits-and-permissions` (#291, #292, #293, #295, #297, #339, part of #294): bound query parameters reached the terminal through gorm's default logger; the opt-in online PNG fallback had neither a timeout nor a size limit; six directories were created 0777; Excel reads accepted excelize's 16 GB unzip default; the IPC accept loop spun a warning per iteration on a failing listener and left its socket behind; `PipInstall` took `--requirement=/path` as a package name; and `ipc.WriteMessage` wrote lengths its own reader refuses.
- 2026-09-12 `fix-clear-defects-ccl-finance` (#365, #366, #368, part of #248): a CCL literal too large for a float64 became `+Inf` because ParseFloat's error was dropped; `1e5` did not compile at all, though `VALUE('1e3')` always worked; `TOSTR(1.5, '%d')` wrote Go's own complaint `%!d(float64=1.5)` into the cell; and `finance`'s `RoundUnnecessary` took the program down rather than reporting the rounding it exists to detect. #368 was already fixed by `ccl-error-reporting` and now has a regression test.
- 2026-09-12 `fix-clear-defects-core` (#337, #338, #340, half of #228): eight values that came back wrong with nothing to show for it — a microsecond timestamp read as seconds (2023 became the year 53,872), column names `ParseColIndex` refused at the top of its own range, `Show` printing `9999.99999` as `10000`, a date pattern that rewrote letters inside words (`MMM` to `011`), `IsNumeric` and the numeric read path disagreeing about every named numeric type, a leaked `BiIndex` id, three interpolations answering a NaN with a plausible number, and two unnamed tables that could not be merged because the empty name counted as a duplicate of itself.
- 2026-09-12 `test-low-coverage-packages` (TS-18 of #309): the rest of TS-18. `cli/repl` 39.1% to 72.1% — its whole uncovered half was tab completion, which needs no terminal to test. `stats/internal/fa` 22.2% to 34.3%: the GPArotation family had never been run, and with R unavailable the tests check what the mathematics guarantees — every criterion's analytic gradient against a numerical derivative (all seven correct), orthogonality and communality invariance, unit column norms. `datafetch` 44.5% to 46.6%; the rest of it is the Google Maps crawler (#249) and live HTTP. `ml/mltest` and `accel/knnbridge` left on purpose: one is a test harness, the other is GPU-gated (#303). The rotation tests found [#373](https://github.com/HazelnutParadise/insyra/issues/373) — `Restarts > 1` with an orthogonal rotation returns a non-orthogonal matrix, so the loadings stop describing the same model; the default of 1 is sound.
- 2026-09-12 `lp-never-returns-a-nil-table`: every failure path in `SolveFromFile` and `SolveModel` returned a nil first table, and `Docs/lp.md`'s own example calls `result.Show()` on it. Both returns are now empty but usable tables carrying the reason. Deliberately **not** the LP-2 redesign to `(*Solution, error)`: its hard part is parsing GLPK's output into variables, which cannot be verified here (no `glpsol` on this machine or in CI), and its error shape depends on LP-1 (#257) — so the crash is gone with no signature change, and the redesign stays one decision rather than two breaks. LP-2 has no issue of its own, unlike LP-1 and LP-3.
- 2026-09-12 `fix-silent-wrong-answers`: three defects that returned something plausible rather than failing. `Show`'s formatter printed `2^63` as `9223372036854775807` on a Mac and `9.2234e+18` on Linux — the same `int(f)`-is-undefined trap `ccl-portable-integer-arguments` fixed in CCL the same day, now checked against `int64`'s range before converting. A millisecond timestamp past 2262-04-11 read as a different date, because the conversion multiplied into nanoseconds and overflowed inside the range it accepts. And `isr.DT.From(map[int]any{…})`, documented as supported, always produced an empty table: it stringified the key to "0" where an Excel index was wanted. Verified on both architectures.
- 2026-09-12 `never-panic-on-bad-chart-input`: eight paths that panicked under the default config, against an `error-philosophy` requirement that already forbade it. The one that mattered most was `gplot.CreateBarChart(BarChartConfig{}, data)` — a zero-value config, the first thing anyone writes. Also a nil function, a ragged heat-map grid, a negative colour count, a nil `IDataList` in four `plot` constructors, a `SavePNG` path with no extension, a LINGO declaration with reversed parentheses, and a negative width in `TruncateString`. All eight were found by the first tests these packages ever had.
- 2026-09-12 `test-untested-packages` (#307, TS-13 of #309): twelve packages that had no test file now have one, and the two the README points at came up with them — `plot` 0% to 72.6% and `isr` 44.2% to 85.7%. Also `gplot` 24.8% to 79.5% (every chart now rendered to a file, not merely built), `internal/utils` 34.6% to 91.1%, `lpgen` 0% to 83.9%, the five `engine/*` re-exports, `stats/internal/parutil`, `datafetch/internal/limiter` and `cli/style`. TS-18 is three of eight done; the rest is listed under Next. Writing them found six defects, fixed in their own changes: seven panics across `gplot` and `plot`, one in the LINGO parser, `isr.DT.From(map[int]any{…})` never producing a table, and two wrong answers in `internal/utils`.
- 2026-09-12 `parquet-filter-keeps-every-batch`: `FilterWithCCL` returned the first 1000 matching rows and silently dropped the rest — it read in batches and appended every batch after the first into `GetColByNumber`'s copy of the column. A 2500-row file filtered on a condition every row satisfies came back with 1000 rows and no error. The first test ever written against `parquet/ccl.go` found it. In the same loop: a read error arriving after the last batch could be traded for a partial result (the record and error channels close together and `select` picks at random), which in `ApplyCCL` meant overwriting the original file with the truncated one.
- 2026-09-12 `test-unpinned-behaviour` (#304, #305, #306, #308): the documented left and right joins and `SortBy`'s stability promise now have tests behind them, and the three packages whose exported surface had never been run directly do too — `internal/core` 20.1% to 92.5%, `parquet` 21.2% to 73.6%, `lp` 2.2% to 11.4% (the rest of `lp` needs `glpsol`). Nothing in the library changed. Writing them turned up three defects that did not: a Parquet column type the reader does not know reads back as the same string in every row, `lp.SolveFromFile` hands back a nil table its own documented example dereferences, and the streaming read can drop a mid-file error (fixed in `parquet-filter-keeps-every-batch`).
- 2026-09-12 `ci-hygiene` (#277, #278, #279): CI now runs on `0.4`, where none of the review's batches had run before. The lint job checks gofmt (24 files formatted), the Test job vets on every OS, runs `-race` on ubuntu and reports coverage from macOS (the two together timed out on the clustering tests), every workflow declares least-privilege permissions, and deploy-docs no longer persists credentials it does not use. The first run found three failures no Mac could show: two tests asserting POSIX permission bits on windows, and the next item.
- 2026-09-12 `ccl-portable-integer-arguments`: CCL counts, digit counts, date shifts and row range bounds too large for Go's int or Duration conversion no longer answer differently on amd64 and arm64 (`MID('abc', 2, 10^300)` was `""` on Linux and Windows, `"bc"` on a Mac). Reproduce amd64 locally with `GOARCH=amd64 go test` under Rosetta.
- 2026-09-11 `cli-reject-ignored-args`: `accel` and the nine DataList statistics reject an argument they do not use (`mean x as m` printed the mean and stored nothing). `accel --precision`, read by nothing since `accel run` was removed in v0.3.1, is gone from the parser, the Cobra flags and the Usage. An audit found 53 more commands that ignore a trailing argument; they wait on a per-command argument-count rule.
- 2026-09-11 `match-integers-by-value`: every search, count, replace and drop by value, and every encoder category, matches integers by value whatever their Go type, so the `int64` 2s a CSV load produces are found by `2`; floats stay separate. It was a library bug the CLI inherited (`count x 2` printed 0). The comparison is picked once per call, which also made a million-cell float `Count` go from 2.3 to 1.5 ms and a string one from 2.7 to 2.0 ms.
- 2026-09-11 `cli-docs-match-registry` (#321): every command's Usage in `Docs/cli-dsl.md`, `cli-command-usage.md` and `cli-command-guide.md` equals its `help` text, and both topic lists name every command, `accel` included; two tests enforce it. `pca`, `regression` and `count` got Usage strings that match what they parse, and the guide's placeholder examples were replaced with invocations that were run.
- 2026-09-11 `core-lock-and-snapshot`: `AtomicDoAll` takes `...Lockable` (a wrong type no longer compiles, a nil instance is skipped); `GroupBy` copies the data it groups; `ExecuteCCL` is all or nothing; custom CCL functions are documented with a locking rule measured under `-race` and a mirror-deadlock test; tests restore the global `Config` they change.
- 2026-09-11 `format-numbers-as-text`: CCL strings, `ToCSV`, `ToStringSlice` and generated column names write a float by `encoding/json`'s rule instead of `%v`, which switched to exponent form at one million.
- 2026-09-10 `ccl-performance`: row-invariant aggregates are folded before the row loop (20k-row z-score 6.0 s to 2.1 ms); date-probe fast path, regex cache, and a rolling conversion hoist. 66,000 values compared bit for bit before and after.
- 2026-09-10 CCL documentation gaps (#356, documentation only): case rules, `AVG` versus `SUM`/`COUNT`, how a number becomes text.
- 2026-09-10 `ml-classes-never-nil`: the ten `Classes()` methods return an empty list carrying the reason; `ml/mltest` rejects a third-party `Classes()` that returns nil.
- 2026-09-10 `enforce-chainable-never-nil`: `TestChainableMethodsNeverReturnNil` parses the module and fails on a chainable method that returns nil (182 methods, none violating).
- 2026-09-09 `ccl-error-reporting` and `ccl-row-slice-values`: typed `CompileError`/`EvalError` with byte offsets and row numbers, reachable through `errors.As(dt.Err(), …)`; a bare column range and `LAG(@, n)` resolve against the current row.
- 2026-09-09 `refresh-deps-before-release` and `bump-grpc-xds-advisory` (on `dev`, merged here): dependencies are refreshed before every merge to `main`; grpc moved past its xDS advisory.
- 2026-09-09 `fix-api-review-batch-8`: nine CCL defects that produced a value for an expression that could not mean what it said. `&&`/`||`/`CASE` short-circuit; argument lists require commas; strings compare as text and a word against a number is an error; `nil` concatenates as the empty string; `&` binds looser than `+`/`-` and the docs gained a precedence table; a bare range and `LAG(@, …)` are errors; `AND()`/`OR()` check their arguments (the unreachable duplicate implementations are deleted); indices and windows must be whole numbers; fractional days keep sub-hour precision.
- 2026-09-08 `fix-api-review-batch-7`: six CLI defects where a command reported success after doing nothing. Shared target pre-checks, closed-set option parsing, `config` key and value validation, usage text matching the command, rejected extra arguments, range checks.
- 2026-09-08 `fix-api-review-batch-6`: I/O corruption. Every charset the detector can report now has a decoder and an undecodable file is an error rather than raw bytes in a cell; UTF-32 BOM recognised; SQLite identifiers quoted; `ToCSVWithOptions` with opt-in formula sanitising.
- 2026-09-07 `fix-api-review-batch-5`: six silently-wrong-answer defects. Exact integer ordering above 2^53, the Hermite basis, `KMeans` initial centres, `TryParseTime` layouts, `ShowTypes` column order, `Close()` dropping queued work. The non-breaking ones are backported to `dev`.
- 2026-09-07 `make-errors-non-terminating`: the library never terminates or panics by default; `Config.SetPanicOnError(true)` is the opt-in; `Err()` is sticky with `PopErr()`; chainable transforms never return `nil`.
- 2026-09-06 `fix-api-review-batch-4`: 21 high-severity, decision-free issues closed (CCL correctness and panics, atomic `ToCSV`/`ToJSON`, `AtomicDoAll` nested deadlock, csvxl sheet-name traversal, CLI nil-variable crash / NaN persistence / root flags / script REPL / password masking, empty and never-failing tests, CI `-run` pattern, tracked test binary).
- 2026-09-06 `bump-vulnerable-deps`: thrift, grpc, x/image, x/crypto past their advisories; `go` directive unchanged.
- 2026-09-06 `fix-api-review-batch-3`, `-2`; 2026-09-05 `-1`.
- 2026-09-05 quant: risk metrics, beta/CAPM, factor model, options pricing, portfolio optimisation, block bootstrap; CLI `quant` forms; datafetch TWSE adjusted prices; timeseries basics and CLI commands.
- 2026-08-06 `add-accel-execution-logging`, `add-nn-sequential-fit` archived.

## Blockers
- Multi-GPU wall clock and non-Apple GPU parity still need hardware nobody has (see AGENTS.md follow-ups).
- GPU verifications run only where a device exists, so they stay manual; `ENG.md` says how to run them. The MNIST and real-model gates no longer wait on hardware — CI fetches their data (nn-data-gates-in-ci).

## Next Verifiable Output
An owner decision on the next fork taken, then that change's tests green under `go test ./...`, `go test -race` on the touched packages, and `golangci-lint run`, with the issue closed and the `api-review.md` row marked. #371 and #373, the two wrong answers, are both done. There is no batch to start before a decision lands.

## Next OpenSpec Change
Owner decision needed: pick from the decision-fork list above. Most of the remaining `severity:med` issues are API redesigns that need the same kind of decision. The test-coverage group (#304–#309) is finished. The decision-free backlog is empty and everything left needs a ruling. Both defects the review's own tests turned up are now closed.

## Decision Delta Since Previous Handoff
- The two unmerged legacy branches stay as they are: `copilot/fix-aa06638c-…` (Copilot's LINGO `@SUM`/`@FOR` support, its PR #74 closed unmerged) and `lingo-to-lp` (an early LINGO lexer and processor from 2024). The owner ruled on 2026-09-26 to neither delete, archive nor revive them.
- CSV output guards formula-like text by default; the owner chose protection over byte-exact round trips on 2026-09-26 (#285), once numbers and number-only text were exempt. A program that must read back exactly what it wrote sets `AllowFormulas`.
- `finance.Zero` stays an exported variable: the owner ruled on 2026-09-26 (#248) that nobody reassigns it, and making it a function would rewrite every `finance.Zero` a caller has written. The two other halves of #248 were already fixed (the `RoundUnnecessary` panic, and the last-`Options`-wins rule, now an error).
- An exported function in Insyra is declared with `func`, never held in a variable, and has one name. Where two names existed, the one in the `Read*` family stays: the owner chose `ReadSlice2D` over `Slice2DToDataTable` on 2026-09-25, with the other kept one release as Deprecated, the schedule `SetDontPanic` set (exported-functions-are-functions).
- Saving to Excel writes one named sheet and never touches the others. An existing sheet is refused by default and replaced only on request, in its original position. Overwriting the whole file, which is what `save` does for CSV, JSON and Parquet, was rejected because a workbook holds sheets the table never came from: loading sheet 2025 of a three-year report and saving it back would have deleted 2023 and 2024 without a word. The owner chose sheet-level writes with a refusing default on 2026-09-25, and the sheet name defaults to `Sheet1`, the recommendation, which the owner did not override. A save with no sheet name does not pick the next free name (`Sheet2`, `Sheet3`) either, as the owner confirmed the same day: a script run twice would pile up copies of the same data with nothing saying which is current, and `load` needs the name, which the caller would not know (save-writes-excel-sheets).
- Reading and writing take a source and a destination, not only a path: every format in `insyra` and `parquet` has a reader and writer entry point, and the path and string versions call it. Renames were deferred to #213 on 2026-09-25 so the same functions' names and parameters change in one release, and `csvxl` stays path-based (reader-writer-entry-points).
- CI lints beyond golangci-lint's default set for resources left open and errors lost (`nilerr`, `bodyclose`, `rowserrcheck`, `sqlclosecheck`, `errorlint`), and not for `gosec`, `unparam` or `noctx`, which were measured and judged mostly noise for this codebase (lint-catches-leaks-and-lost-errors).
- Streaming APIs in Insyra are range-over-func iterators, not channel pairs. `parquet.Stream` moved on 2026-09-24 with no transition version, because its old shape carried the leak (parquet-stream-is-an-iterator). Any new streaming entry point — the CSV one waiting on #210 included — should take the same shape.
- `csvxl` adds `.csv` only where the caller left a gap: on the way in when the path as written names nothing, on the way out when the name has no extension. The owner chose both-ends-with-respect over dropping the convenience on 2026-09-24, and the literal-first reading came from asking what happens to someone who today passes `export.txt` for a file called `export.txt.csv`: an extension check alone would have broken them, a lookup that tries the written path first does not (csvxl-respects-file-extension).
- One column selector for the whole library, decided on 2026-09-22 after the owner asked for full unification rather than the minimal fix. Unifying on name-first was rejected because it would make a call's meaning depend on the data; typed `ColIndex`/`ColName` string types were rejected because Go converts an untyped literal implicitly, so `GetCol("price")`, the case T-11 reported, would still have compiled. The generic `any` parameter keeps every existing `"A"` literal compiling, and the explicit `ByIndex`/`ByName`/`ByNumber` spellings stay for callers who prefer to say it in the method name (one-column-selector).
- A bare word in CCL is a column index, never a column name, and the owner confirmed on 2026-09-21 that this was always the design. Name-first resolution was rejected because it would make an expression's meaning depend on what the columns are called, so renaming a column could silently change a result. The column names now shape only the error message, which is what keeps the strict rule cheap to live with (ccl-identifier-is-only-an-index).
- The data-gated `nn` checks run in CI on changes under `nn/` and on manual dispatch, with no schedule; the owner ruled out a weekly run on 2026-09-20. A self-hosted runner on the owner's machine was not proposed, because the repository is public and fork pull requests would run on it. The GPU gates stay manual for want of a runner with a device, and `ENG.md` now says so rather than leaving them looking covered (nn-data-gates-in-ci).
- A test that claims one code path reproduces another compares the two runs, not a transcription of one of them. f32 arithmetic differs between arm64 and amd64, so recorded digits pin the machine that recorded them; the MNIST sugar proof now runs both paths in the same process (mnist-proof-compares-runs).
- Factor analysis parity against psych is held strict by what a rotation cannot change (L·Phi·L', S·L', W·L'), not by where along a flat criterion minimum either side stopped. The frame fields of the same solution share one tolerance, `factorRotationTol` = 1e-2, measured from GPArotation's own same-minimum spread; per-combination tolerances from psych's seed-to-seed drift were tried and rejected, because psych keeps the identity start most of the time and its drift understates what the criterion allows (factor-parity-compares-invariants).
- Gradient projection rotations follow GPArotation's current default algorithm rather than its old step, and there is no option to choose the old one: nothing in the library exposed the algorithm, and no measured case needed it. The reference test compares the first six iterations step by step and the end by convergence, criterion and loadings, not by iteration count, because rounding carried through the step size moves where a long run stops on every platform, R's included (rotations-use-gparotation-bb).
- `insyra/parallel` stays. It exists so people who would not write wait groups can run data computations at once, and its `GroupUp(...).Run().AwaitResult()` shape is deliberate; the review's advice to deprecate it in favour of `sync.WaitGroup` was rejected by the owner on 2026-09-13 (#271).
- A group is a reusable list of functions, and each `Run` returns a separate run that can only be awaited, so awaiting before running does not compile and running twice gives two independent results. `AwaitResult` returns the error directly rather than through an `Err()` method, which costs the one-line `a, b := r1.AwaitResult(), r2.AwaitResult()` but cannot be forgotten (parallel-runs-and-worker-errors).
- The random rotation starts come from a fixed seed, not from the loadings. A seed taken from the data is only as portable as the data's last bits, and extraction does not reproduce those across architectures. A platform-stable function of the data (rounded loadings, the dimensions) was rejected: rounding still has boundaries a 2e-8 difference can cross, and neither psych nor GPArotation ties its starts to the data. The CI test that caught it stays unchanged, because its claim is true and its count of five starts is now the same draw on every platform (rotation-starts-same-on-every-platform).
- A `csvxl` batch keeps converting past a CSV that fails and names every failure, rather than saving nothing. The owner chose batch tolerance on 2026-09-13 (#267). What tolerance may not do is leave damage: a failed file gets no sheet, a sheet it would have replaced keeps its content, and nothing is saved when nothing converted (csvxl-batch-failures).
- `lp` defaults to a solver written in Go instead of installing GLPK, and keeps GLPK as an engine the caller installs and selects. The owner ruled both on 2026-09-13 (lp-pure-go-default).
- `SolveModel` and `SolveFromFile` are replaced on `0.4` with no deprecated wrappers. Their two-table return could not tell "the model has no solution" from "the solve failed", so a wrapper would have kept the defect #372 is about (lp-pure-go-default).
- The LP reader copies GLPK's behaviour, including prefix keywords and a Binary variable keeping its explicit bounds, so one file is one model to both engines. It departs only where GLPK silently reads a malformed bound as a different model (lp-pure-go-default).
- A sort config that gives more than one selector sorts by precedence (index, name, number) and logs a warning; it is not an error, because the index-before-name rule is documented and followed across the library (sortby-column-selection).
- A sort config that names no column sorts by the first column, documented and without a warning; it is not refused, because `SortBy` is only called by someone who wants a sort (sortby-empty-config-first-column).
- `ColumnIndex`'s fallback to an uppercased column name, shared by `GetCol`, `SetColToRowNames` and `SortBy`, was removed by `one-column-selector`, which also gave `SetColToRowNames` the name-based counterpart the ruling said it would need. The 2026-09-13 ruling is the narrow half of the 2026-09-22 one.
- The exact decimal type across Insyra is `github.com/TimLai666/go-decimal`, kept and recommended to users library-wide rather than swapped for `shopspring/decimal`. Checked against the current versions of shopspring, apd and govalues on 2026-09-13; the comparison is in `Docs/Decimal.md` and the standing decision in `ENG.md`. This settles FI-1 of #247 as a recorded decision; FI-2 was fixed by `a-decimal-is-a-number`.
- The test for "can this be a map key" is self-equality, not comparability. Comparability is necessary (== must not panic) but not sufficient (== must mean something), and the two differ exactly on NaN and on anything containing one (nan-is-not-a-map-key).
- `NewDataList`'s flattening of slices is deliberate and stays, including for `[]byte`. The escape hatch is per-argument (`Cell(v)`) rather than a change to the rule, so the pandas-like reading of the constructor is untouched (one-value-one-cell).
- `ToMapKey` was named twice before it settled. `KeyOf` promised "the key for this value" and delivers the key for this *Go* value, which differs on integer width; `CounterKey` bound a general helper to its first caller, when a caller's own map, set or index hits the same panic. All three renames landed before release (one-value-one-cell, identify-uncomparable-cells).
- A cell Go cannot compare is identified by its type and content, and that one rule covers counting, searching, matching and grouping. The alternative considered and rejected was a new comparable cell type for bytes (`type Bytes string`): it would have left `[]byte` and `Bytes` as two unequal ways to say the same thing, failed the type assertion people write by habit, and still not removed the `Counter` panic, because `[]int` cells reach it through `Append` regardless (identify-uncomparable-cells).
- `Counter` keeps `map[any]int`. A stand-in key was chosen over changing the signature to carry the original value: comparable values keep keying by themselves, so `counter[1]` is untouched, and nothing breaks for the ordinary case (identify-uncomparable-cells).
- Whether `Counter` should merge `int(1)` and `int64(1)` is still open and deliberately untouched (identify-uncomparable-cells).
- A Parquet column type the reader cannot represent reads as `nil` with the reason on the table rather than as plausible text, and the set of types it can represent was widened first so the refusal applies to a small residue. Reading each cell as its own string via `arr.ValueStr(i)` was measured to work for every type tried, and rejected: a decimal money column would become a text column with no signal, which is the silent-downgrade class this review exists to remove (parquet-foreign-column-types).
- A decimal is stored exactly, orders by value, and is **not** a number to `IsNumeric`, following the `time.Time` precedent rather than widening what numeric means library-wide. Whether that is right for money is recorded as a follow-up (parquet-foreign-column-types).
- `github.com/TimLai666/go-decimal` is a core dependency, not a `parquet` one, because ordering needs `decimal.Cmp` in `internal/algorithms` and package functions cannot be reached through an interface (parquet-foreign-column-types).
- A rotation start must lie on the criterion's own manifold, and the minimal form of that is the one `GPArotation` already uses: every start orthogonal, for the oblique family as well as the orthogonal one. Threading the start into `GPForth`/`GPFoblq` as its `Tmat` instead of premultiplying the loadings was considered and rejected as a refactor without a correctness argument — with an orthogonal start the two are the same optimisation (`T = S·R` gives `L·(T')⁻¹ = L·S·(R')⁻¹` and `T'T = R'R`), so it would only admit start matrices `GPArotation` does not use (orthogonal-rotation-starts).
- Among restarts, a converged solution beats one that did not converge whatever their criterion values; comparing criterion values only means something among runs that finished. When none converged the best is still returned, reported as not converged, rather than failing (orthogonal-rotation-starts).
- `oblimin` uses its starts like every other method. Decided on what `Restarts` means and on both R references, not on the comment's SPSS claim: SPSS is single-start and that is `Restarts: 1`, which stays bit-identical. No threshold before a random start may displace the identity, because neither `GPArotation`'s engine nor `psych::faRotations` has one (oblimin-honours-its-start).
- Among starts, the lowest criterion value among those that converged wins, not psych's hyperplane count: the criterion defines the rotation, `GPArotation`'s engine and `fungible::faMain` both select its minimum, and measured on three fixtures with twenty starts the two rules chose the same basin every time, including the over-factored one (default-restarts-follow-psych).
- `Restarts` defaults to 20, psych 2.6.5's `n.rotations`, because psych is the reference implementation and a single start is the estimator only when the criterion is unimodal; SPSS and `GPArotation` stay reachable as `Restarts: 1`. The informed Varimax start runs at the rotation's own tolerance, which is where the 85 ms default cost was (default-restarts-follow-psych).
- `AtomicDoAll` narrows to a closed `Lockable` interface rather than keeping `...any` with a louder runtime error: a value that cannot be locked is a mistake the compiler can catch (core-lock-and-snapshot).
- A custom CCL function that reads another table is made safe by locking every table involved from outside the CCL call. Locking from inside the function was measured to deadlock against the mirror image, so it is documented as the thing not to do (core-lock-and-snapshot).
- `ExecuteCCL` gets no `ExecuteCCLErr`: typed errors already reach callers through `errors.As` (core-lock-and-snapshot).
- A number written as text follows `encoding/json`'s rule. Sort keys, cache keys and `Show`'s display are deliberately excluded, and a `nil` category keeps the documented name `<nil>` (format-numbers-as-text).
- `ROLLING_*` stays O(rows × window): a running accumulator would change results by drift where recomputing does not (ccl-performance).
- `Classes()` returns an empty list with a reason rather than changing its signature, because `Classifier` is a public interface; consumers keep a nil guard since third parties can still return nil (ml-classes-never-nil).
- CCL boolean coercion stays and the documentation was corrected, rather than the reverse: `IF`, `AND`, `OR`, `CASE`, `&&` and `||` all read their condition the same way, so a strict `&&` would have split the language (batch 8).
- CCL comparison resolves in one order — numbers, then text, then an error — so `'10' > '9'` keeps its documented numeric reading while `'abc' < 'abd'` gains a real one (batch 8).
- `ColumnRange`/`RowRange` stay internal and an expression that ends with one is an error, instead of exporting them so callers can type-assert an implementation detail (batch 8).
- A CLI check reads `ColNames()`/`RowNames()` rather than the `Get*` lookups, because a check must not record an error on the user's table; `checkTableErr` uses `PopErr` so a sticky error cannot fail the next REPL command (batch 7).
- `CSVWriteOptions.SanitizeFormulas` defaults off: it changes the value written, and `ToCSV`'s output stays as it was (batch 6, flagged on #285 for the owner to overrule).
- `AtomicDoAll` inside an `AtomicDo` on one of its instances now runs inline without locking the others (trust-zone rule), instead of deadlocking.
- CCL keywords are case-insensitive; out-of-range Excel references are errors; aggregates skip `NaN`; `MapContext` orders columns by name.
- `plot.SavePNG` online fallback is opt-in (batch 3).
