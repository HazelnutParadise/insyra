# Tasks

## 1. Rename

- [x] 1.1 Package tree `dl` → `nn`: directory, package clauses, all imports, accel wiring and tests
- [x] 1.2 Env vars `INSYRA_NN_*` → `INSYRA_NN_*` in code, tests, fixtures, docs
- [x] 1.3 Docs page, indexes, both README tables, changelog Unreleased sections, skills, ENG.md diagram, delivery-status current-state prose; OpenSpec current specs `dl-*` → `nn-*`

## 2. Proof

- [x] 2.1 No `insyra/dl` import remains; full suite (all gates) passes under `nn`
