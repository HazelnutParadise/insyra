# Tasks: csvxl-formula-guard

- [x] 1.1 測試先紅：`ExcelToCsv`、`EachExcelToCsv` 預設防護、`AllowFormulas` 原樣、`Sheets` 篩選與不指定時全部、兩包報錯；CLI `convert` 預設防護、`allowformulas true`、未知參數報錯、用在 csv->xlsx 被拒。
- [x] 1.2 `ExcelToCsvOptions`、共用 `GuardFormula` 與 `selectSheets`；CLI `convert`。
- [x] 2.1 `Docs/csvxl.md`、`Docs/cli-dsl.md`、CLI skill、兩份 CHANGELOG。
- [x] 2.2 全套驗證、歸檔；在 #285 回報。
