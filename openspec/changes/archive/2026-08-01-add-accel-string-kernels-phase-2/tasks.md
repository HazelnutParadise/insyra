# Tasks

Withdrawn. Nothing was implemented and nothing will be.

- [x] W.1 Record why the boundary this change drew no longer means anything, in the proposal
- [x] W.2 Record the withdrawal in `delivery-plan.md` as a decision, so the reasoning survives the archive
- [x] W.3 Note that the encoded-string transport remains and is unused, so a future string operation knows it is there
