# Tasks: csv-formula-guard-by-default

- [x] 1.1 測試先紅：預設防護、數字與純數字文字不動、`AllowFormulas` 原樣；CLI 預設防護、`allowformulas true` 原樣、用在 JSON 被拒。
- [x] 1.2 防護只處理不是單純數字的文字；欄位改成 `AllowFormulas`，`SanitizeFormulas` 保留一版當 Deprecated 無作用欄位；CLI 選項。
- [x] 2.1 `Docs/DataTable.md`、`Docs/cli-dsl.md`、CLI skill、兩份 CHANGELOG、`AGENTS.md` follow-up。
- [x] 2.2 全套驗證、歸檔、寫 Purpose；在 #285 留言。
