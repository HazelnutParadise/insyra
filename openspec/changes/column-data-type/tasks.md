# Tasks: column-data-type

## 1. 實作
- [x] 1.1 測試先紅：數字（含缺值、Inf、decimal、自訂數值型別）、文字（含像數字的文字）、布林、時間、其他、混合、全缺值、空清單；`ColDataTypes` 依欄位順序；`String()`。
- [x] 1.2 `DataType`、`DataList.DataType()`、`DataTable.ColDataTypes()`，加進兩個介面。

## 2. 文件與紀錄
- [x] 2.1 `Docs/DataList.md`、`Docs/DataTable.md`、`skills/insyra/SKILL.md`；兩份 CHANGELOG。
- [ ] 2.2 全套驗證；`delivery-status.md`；歸檔、寫 Purpose。
