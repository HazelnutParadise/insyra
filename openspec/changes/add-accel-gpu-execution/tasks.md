## 1. Implementation
- [x] 1.1 Validate the change with `openspec validate add-accel-gpu-execution --strict`
- [x] 1.2 Write the failing runtime tests first: a fake executor registered through the backend registry covering routing to a registered backend, missing-backend fallback, empty column, all-null column, `float64` refusal, backend-error reporting, and strict-mode error propagation
- [x] 1.3 Replace `BackendAllocator` with a `BackendExecutor` seam that carries the operation, a `context.Context`, the projected column, and returns the computed result, an error, and measured transfer/dispatch/readback durations
- [x] 1.4 Delete `accel/allocator_builtin.go`, the three fabricated per-backend transfer constants, and the `execution.bytes_moved` telemetry derived from them, and remove them from CLI output
- [x] 1.5 Add `float32` eligibility and the new fallback reason codes (`dtype-not-eligible`, `shader-compile-failed`, `buffer-too-large`, `readback-timeout`, missing-backend), and make the tests from 1.2 pass with no real backend present
- [x] 1.6 Create the `accel/backend/wgpu` Go module depending on `github.com/gogpu/wgpu`, registering both a device probe and an executor through the existing registries on blank import
- [x] 1.7 Implement the `float32` column-sum kernel: WGSL workgroup reduction, chunked to stay under the device storage-buffer limit and the 65535 workgroup-count limit, with staging-buffer readback and a per-process pipeline cache
- [x] 1.8 Reject the backend's software/CPU adapter (`DeviceType == DeviceTypeCPU`, `Name == "Software Renderer"`) so a host with no GPU driver falls back to the CPU path instead of reporting a SPIR-V interpreter as an acceleration device, with a test
- [x] 1.9 Classify shared versus discrete memory from vendor and backend rather than the adapter-reported device type, covered by a test, because `adapter.Info()` reports an Apple M3 as `DiscreteGPU`
- [x] 1.10 Add the real-hardware numeric test asserting that the GPU sum equals the CPU sum, gated behind `INSYRA_ACCEL_GPU_TESTS=1`
- [x] 1.11 Add a benchmark that produces measured transfer, dispatch, and readback timings for the reduction, and record the resulting table in `design.md`; use host wall-clock around submit and map, since Metal and GLES return `ErrTimestampsNotSupported` and only Vulkan and DX12 implement GPU timestamp queries. Scoped down from the original wording: a compute-bound kernel would need a second WGSL shader, which is outside this change's one-operation scope. The compute-bound figure in `design.md` comes from a standalone measurement, not from a benchmark in this repo, and is labelled as such.
- [ ] 1.14 Add a compute-bound kernel benchmark once a second operation exists, so the profitability rule can be fitted against both ends of the arithmetic-intensity range rather than one
- [x] 1.12 Update `Docs/accel.md`, README and README_TW, CHANGELOG and CHANGELOG_TW, and the `skills/insyra` and `skills/use-insyra-cli` references to describe the opt-in backend module and the `float64` restriction
- [ ] 1.13 Run the numeric test on at least one non-Apple host (Windows or Linux, NVIDIA or AMD) before archiving; until that passes, document the backend as verified on macOS and Metal only
